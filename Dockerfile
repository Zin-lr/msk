# Use the base image
FROM springcloud/spring-cloud-dataflow-server:2.11.2-SNAPSHOT

# Optionally, you can add additional configuration or dependencies here
# For example:
# COPY additional-files /app/additional-files

# Set any environment variables if needed
# ENV VARIABLE_NAME=value

# Expose any ports if required
# EXPOSE port_number

# Define the command to run your application
# CMD ["command", "arg1", "arg2", ...]
